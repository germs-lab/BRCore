# Check if running in an HPC environment

Check if running in an HPC environment

## Usage

``` r
is_hpc_environment()
```

## Value

Logical indicating whether we're in any HPC environment

## Examples

``` r
is_hpc_environment()
#> [1] FALSE
```
